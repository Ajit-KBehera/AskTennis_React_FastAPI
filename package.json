{
    "name": "asktennis-root",
    "version": "1.0.0",
    "description": "Root package for AskTennis project",
    "private": true,
    "scripts": {
        "backend": "python3 backend/main.py",
        "frontend": "npm run dev --prefix frontend",
        "predev": "brew services start redis || echo 'Redis start skipped/failed'",
        "dev": "concurrently \"npm run backend\" \"npm run frontend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
